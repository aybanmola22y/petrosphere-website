/**
 * Used only when `NODE_ENV !== "production"` and `ADMIN_PASSWORD` is unset.
 * Set `ADMIN_PASSWORD` in production — this fallback is disabled there.
 */
export const DEV_FALLBACK_ADMIN_EMAIL = "admin@petrosphere.local";
export const DEV_FALLBACK_ADMIN_PASSWORD = "petrosphere-dev";
